const tabButtons=document.querySelectorAll('.tab-btn');const scheduleDays=document.querySelectorAll('.schedule-day');tabButtons.forEach(btn=>{btn.addEventListener('click',()=>{tabButtons.forEach(b=>b.classList.remove('active'));scheduleDays.forEach(d=>d.classList.remove('active'));btn.classList.add('active');document.getElementById(btn.dataset.day).classList.add('active');});});const players=[{name:'Nick Brown',team:'ozark'},{name:'Dad',team:'ozark'},{name:'Brother 1',team:'valley'}];const playerList=document.getElementById('playerList');players.forEach(p=>{const div=document.createElement('div');div.textContent=`${p.name} (${p.team})`;playerList.appendChild(div);});const sponsors=['Sponsor 1','Sponsor 2'];const sponsorGrid=document.getElementById('sponsorGrid');sponsors.forEach(s=>{const div=document.createElement('div');div.textContent=s;sponsorGrid.appendChild(div);});const form=document.getElementById('signupForm');const msg=document.getElementById('formMsg');form.addEventListener('submit',e=>{e.preventDefault();msg.textContent='Thanks for signing up!';form.reset();});